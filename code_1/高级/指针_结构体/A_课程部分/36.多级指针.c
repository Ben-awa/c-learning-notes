//1.
//#include <stdio.h>
//
//int main() {
//    int a = 20;
//    int  * pa = &a; //指向普通变量的指针
//    //因为现在要指向一个int *类型的变量，所以类型为int*  再加一个*
//    int ** pp = &pa; //指向指针的指针（二级指针）
//    int *** ppadsadp = &pp;  //指向指针的指针的指针(三级指针)
//    printf("%d\n", *** ppadsadp);
//    //貌似，命名随便名（变量名）
//
//    printf("%d\n",**pp == *pa); //得1,即 *pp == pa, **pp== *pa==a的值
//
//}

//2.一级指针可以操作一级数组，但二级指针显然不能操作二维数组!
